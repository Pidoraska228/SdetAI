#include "filesystem.hpp"
#include <sstream>
#include <regex>

namespace sdetai {

// All implementations are in the header (header-only for templates and inline)
// This file exists for completeness and future non-inline implementations

} // namespace sdetai